<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.benet.collect.mapper.CctQuestoptsMapper">

    <resultMap type="CctQuestopts" id="CctQuestoptsResult">
        <result property="id" column="id"/>
        <result property="optNo" column="opt_no"/>
        <result property="optTitle" column="opt_title"/>
        <result property="optDesc" column="opt_desc"/>
        <result property="questNo" column="quest_no"/>
        <result property="orderNo" column="order_no"/>
        <result property="optScore" column="opt_score"/>
        <result property="checkState" column="check_state"/>
        <result property="branchNo" column="branch_no"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleteFlag" column="delete_flag"/>
        <result property="comments" column="comments"/>
        <result property="appCode" column="app_code"/>
        <result property="version" column="version"/>
    </resultMap>

    <sql id="selectCctQuestoptsVo">
        select id, opt_no, opt_title, opt_desc, quest_no, order_no, opt_score, check_state, branch_no, create_by, create_time, update_by, update_time, delete_flag, comments, app_code, version from cct_questopts
    </sql>

    <select id="getAllRecords" resultMap="CctQuestoptsResult">
        <include refid="selectCctQuestoptsVo"/>
        where delete_flag='1' and app_code=#{appCode}
    </select>

    <select id="getRecordsByClassNo" resultMap="CctQuestoptsResult">
        <include refid="selectCctQuestoptsVo"/>
        where delete_flag='1' and app_code=#{appCode} and quest_no=#{classNo}
    </select>

    <select id="getRecordsByPaging" resultMap="CctQuestoptsResult">
        <include refid="selectCctQuestoptsVo"/>
        where delete_flag='1' and app_code=${appCode}
        <if test="model.condition != null  and model.condition != ''">and ${model.condition}</if>
        order by ${model.orderField} ${model.orderType} limit ${model.pageIndex},${model.pageSize}
    </select>

    <select id="getRecordByNo" resultMap="CctQuestoptsResult">
        <include refid="selectCctQuestoptsVo"/>
        where delete_flag='1' and app_code=#{appCode} and opt_no = #{no}
    </select>

    <select id="getRecordNameByNo" resultType="String">
        select opt_title from cct_questopts
        where delete_flag='1' and app_code=#{appCode} and opt_no = #{no}
    </select>

    <select id="getCountByCondition" resultType="Integer">
        select count(*) from cct_questopts
        where delete_flag='1' and app_code=#{appCode}
        <if test="condition != null  and condition != ''">and ${condition}</if>
    </select>

    <insert id="AddNewRecord" parameterType="CctQuestopts" useGeneratedKeys="true" keyProperty="id">
        insert into cct_questopts (
        <if test="info.optNo != null  and info.optNo != ''">opt_no,</if>
        <if test="info.optTitle != null  and info.optTitle != ''">opt_title,</if>
        <if test="info.optDesc != null  and info.optDesc != ''">opt_desc,</if>
        <if test="info.questNo != null  and info.questNo != ''">quest_no,</if>
        <if test="info.orderNo != null ">order_no,</if>
        <if test="info.optScore != null ">opt_score,</if>
        <if test="info.checkState != null  and info.checkState != ''">check_state,</if>
        <if test="info.branchNo != null  and info.branchNo != ''">branch_no,</if>
        <if test="info.createBy != null  and info.createBy != ''">create_by,</if>
        <if test="info.createTime != null ">create_time,</if>
        <if test="info.updateBy != null  and info.updateBy != ''">update_by,</if>
        <if test="info.updateTime != null ">update_time,</if>
        <if test="info.deleteFlag != null  and info.deleteFlag != ''">delete_flag,</if>
        <if test="info.comments != null  and info.comments != ''">comments,</if>
        <if test="info.appCode != null  and info.appCode != ''">app_code,</if>
        <if test="info.version != null ">version</if>
        )values(
        <if test="info.optNo != null  and info.optNo != ''">#{info.optNo},</if>
        <if test="info.optTitle != null  and info.optTitle != ''">#{info.optTitle},</if>
        <if test="info.optDesc != null  and info.optDesc != ''">#{info.optDesc},</if>
        <if test="info.questNo != null  and info.questNo != ''">#{info.questNo},</if>
        <if test="info.orderNo != null ">#{info.orderNo},</if>
        <if test="info.optScore != null ">#{info.optScore},</if>
        <if test="info.checkState != null  and info.checkState != ''">#{info.checkState},</if>
        <if test="info.branchNo != null  and info.branchNo != ''">#{info.branchNo},</if>
        <if test="info.createBy != null  and info.createBy != ''">#{info.createBy},</if>
        <if test="info.createTime != null ">#{info.createTime},</if>
        <if test="info.updateBy != null  and info.updateBy != ''">#{info.updateBy},</if>
        <if test="info.updateTime != null ">#{info.updateTime},</if>
        <if test="info.deleteFlag != null  and info.deleteFlag != ''">#{info.deleteFlag},</if>
        <if test="info.comments != null  and info.comments != ''">#{info.comments},</if>
        <if test="info.appCode != null  and info.appCode != ''">#{info.appCode},</if>
        <if test="info.version != null ">#{info.version}</if>
        )
    </insert>

    <update id="UpdateRecord" parameterType="CctQuestopts">
        update cct_questopts
        <set>
            <if test="info.optNo != null  and info.optNo != ''">opt_no = #{info.optNo},</if>
            <if test="info.optTitle != null  and info.optTitle != ''">opt_title = #{info.optTitle},</if>
            <if test="info.optDesc != null  and info.optDesc != ''">opt_desc = #{info.optDesc},</if>
            <if test="info.questNo != null  and info.questNo != ''">quest_no = #{info.questNo},</if>
            <if test="info.orderNo != null ">order_no = #{info.orderNo},</if>
            <if test="info.optScore != null ">opt_score = #{info.optScore},</if>
            <if test="info.checkState != null  and info.checkState != ''">check_state = #{info.checkState},</if>
            <if test="info.branchNo != null  and info.branchNo != ''">branch_no = #{info.branchNo},</if>
            <if test="info.createBy != null  and info.createBy != ''">create_by = #{info.createBy},</if>
            <if test="info.createTime != null ">create_time = #{info.createTime},</if>
            <if test="info.updateBy != null  and info.updateBy != ''">update_by = #{info.updateBy},</if>
            <if test="info.updateTime != null ">update_time = #{info.updateTime},</if>
            <if test="info.deleteFlag != null  and info.deleteFlag != ''">delete_flag = #{info.deleteFlag},</if>
            <if test="info.comments != null  and info.comments != ''">comments = #{info.comments},</if>
            <if test="info.appCode != null  and info.appCode != ''">app_code = #{info.appCode},</if>
            <if test="info.version != null ">version = #{info.version}</if>
        </set>
        where delete_flag='1' and app_code=#{info.appCode} and version=#{info.version} and opt_no =
        #{info.optNo}
    </update>

    <delete id="HardDeleteByNo">
        delete from cct_questopts
        where delete_flag='1' and app_code=#{appCode} and opt_no = #{no}
    </delete>

    <delete id="HardDeleteByNos">
        delete from cct_questopts
        where delete_flag='1' and app_code=#{appCode} and opt_no in
        <foreach item="no" collection="nos" open="(" separator="," close=")">
            #{no}
        </foreach>
    </delete>

    <delete id="HardDeleteByCondition">
        delete from cct_questopts
        where delete_flag='1' and app_code=#{appCode}
        <if test="condition != null  and condition != ''">and ${condition}</if>
    </delete>

    <delete id="SoftDeleteByNo">
        update cct_questopts set delete_flag='0'
        where delete_flag='1' and app_code=#{appCode} and opt_no = #{no}
    </delete>

    <delete id="SoftDeleteByNos">
        update cct_questopts set delete_flag='0'
        where delete_flag='1' and app_code=#{appCode} and opt_no in
        <foreach item="no" collection="nos" open="(" separator="," close=")">
            #{no}
        </foreach>
    </delete>

    <delete id="SoftDeleteByCondition">
        update cct_questopts set delete_flag='0'
        where delete_flag='1' and app_code=#{appCode}
        <if test="condition != null  and condition != ''">and ${condition}</if>
    </delete>

</mapper>